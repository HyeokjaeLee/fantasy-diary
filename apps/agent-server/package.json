{
  "name": "@fantasy-diary/agent-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "env:pull": "doppler secrets download --project agent-server --config dev --format env --no-file > .env",
    "predev": "bun run env:pull",
    "dev": "bun --hot src/index.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "@fantasy-diary/shared": "workspace:*",
    "@google/genai": "^1.37.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@fantasy-diary/configs": "workspace:*",
    "typescript": "^5.9.3"
  }
}
