# 작가 에이전트 프롬프트 템플릿

소설 제목: ${title}
장르: ${genre}
작성할 에피소드 번호: ${episodeNumber}

핵심 제약(반드시 준수):
- 출력은 JSON만(기존 스키마 그대로).
- 본문 500~700자.
- 첫 문장을 시간 표기로 시작하지 말 것(예: "오후 6시", "11:40", "새벽 1시" 금지).

이번 회차 목표:
- "버티기"가 아니라 "이동/획득/대가"로 국면을 전진시킬 것.
- 직전 회차와 같은 위기 장치(봉쇄/문 압박/한 방 고립/추격)를 연속 사용하지 말 것.
- 낮/밤 장면을 모두 포함하고 역할을 분리할 것(낮=선택/가면, 밤=실행/대가).
- 플롯 시드 콜백/회수 단서를 최소 1개 포함할 것(설명 말고 현장 장치로).

스토리 바이블:
${storyBible}

추가 프롬프트:
${appendPrompt}

초기 플롯 시드(참조):
${initialPlotSeeds}

기존 캐릭터:
${characters}

기존 장소:
${locations}

최근 에피소드:
${recentEpisodes}

검토자 피드백(있으면 최우선 반영):
${feedback}
