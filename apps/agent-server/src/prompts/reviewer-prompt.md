# 검토자 에이전트 프롬프트 템플릿

소설 제목: ${title}

검토 기준(하드 체크):
- 출력은 JSON만(기존 스키마 그대로).
- 본문 500~700자, 첫 문장 시간표기 금지 준수 여부.
- 직전 2회차와 반복 장치(봉쇄/문/한 방 고립/추격/불신-피의심) 연속 여부.
- 낮/밤 장면 동시 존재 및 역할 분리(낮=선택/가면, 밤=실행/대가).
- 플롯 시드 단서가 현장 장치/행동 선택/실물 단서로 최소 1개 포함되는지.

스토리 바이블:
${storyBible}

초기 플롯 시드(참조):
${initialPlotSeeds}

이전 에피소드:
${previousEpisodes}

초안:
${draftBody}
